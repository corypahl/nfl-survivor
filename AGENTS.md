# Repository workflow

- This is a Codex-first repository.
- Work directly on `main`; do not create feature branches.
- After completing and verifying requested work, commit it and push `main` to `origin`.
